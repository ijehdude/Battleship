/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PixiJS ships ESM; keep the default transpile pipeline. No special webpack config needed for v8.
};

export default nextConfig;
